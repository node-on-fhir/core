{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "Hand-written app-internal schema for the UdapCertificates collection, transcribed 2026-07 from its retired SimpleSchema. createdAt was Date-typed in SimpleSchema; string here because validation runs on a JSON-cloned copy (Date -> ISO string).",
  "type": "object",
  "required": ["resourceType", "certificateOwner", "certificate", "createdAt"],
  "properties": {
    "resourceType": { "const": "UdapCertificate" },
    "_id": { "type": "string" },
    "certificateOwner": { "type": "string" },
    "certificate": { "type": "string" },
    "createdAt": { "type": "string" }
  }
}

# CLAUDE.md — @node-on-fhir/lab-test-reporting

Migrated from Atmosphere `clinical:lab-test-reporting` (2026-06-13). ONC
170.315(f)(3) — lab test reporting to public health. Route `/lab-test-reporting`.
Monorepo-tracked → fresh `git init`; SidebarWorkflows + ClinicianWorkflows →
sidebarItems (`biotech`→`Biotech`, `science`→`Science`); methods-only server.

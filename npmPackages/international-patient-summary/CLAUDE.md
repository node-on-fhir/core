# CLAUDE.md — @node-on-fhir/international-patient-summary

Migrated from Atmosphere `international-patient-summary` (2026-06-13, MIT). IPS viewer + export. Route `/international-patient-summary` (requireAuth). Atmosphere client mainModule was index.jsx; consolidated into self-contained client.js that builds routes/sidebar from workflow.json and preserves named exports (FooterButtons → IPSFooterButtons, SidebarWorkflows, SidebarElements [], DynamicRoutes, IpsContent) + the `Meteor.startup(() => Meteor.IpsContent = IpsContent)` side effect. Server is a single methods.js (wrapped by server/index.js). No Npm.depends; moment peer. `guide/` IG tree skipped. `map`→`Map`. Note: the page lazy-loads `@mlc-ai/web-llm` by injecting a `<script type=module>` whose innerHTML string-imports from `https://esm.run/...` — NOT a bundler import, so Rspack ignores it (no dependency needed). Monorepo-tracked → fresh git init.

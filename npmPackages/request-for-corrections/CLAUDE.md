# CLAUDE.md — @node-on-fhir/request-for-corrections

Migrated from Atmosphere `clinical:request-for-corrections` (2026-06-13, MIT). Patient request-for-corrections / amendments (ONC §170.315(d)(4)). Routes `/correction-requests`, `/correction-requests/new`, `/correction-requests/:id` (all requireAuth). Atmosphere client mainModule was index.jsx (already clean ES-module style); consolidated into self-contained client.js that builds routes/sidebar from workflow.json and re-exports pages/components/collections (CorrectionRequests/Tasks/Communications + CorrectionWorkflow). Kept the original server/index.js mainModule (startup, methods, publications, collections) — but **removed its trailing `if (typeof Package !== 'undefined') { this.X = … }` Atmosphere api.export shim**, which throws in a strict ES module (`this` is undefined); the `export {}` statements already publish those symbols. No Npm.depends; simpl-schema peer. `guide/` (HL7 submodule), `docs/`, `tests/` not copied. `edit`→`Edit`. Already commented out in `.meteor/packages`. Submodule present → fresh git init.

# CLAUDE.md — @node-on-fhir/consent-generator

Migrated from Atmosphere `consent-generator` (2026-06-13, MIT). FHIR consent document generator. Route `/consent-generator` (requireAuth false). **NOT externally gated** — the `clinical:consent-generator` in package.js is a `Package.onTest` self-reference, and the only real deps are `clinical:hl7-fhir-resources`/`hl7-resource-datatypes` declared **`{weak: true}`** (optional; graceful when absent). Atmosphere client mainModule was index.jsx; consolidated into self-contained client.js (route/sidebar from workflow.json) that re-exports `ConsentGeneratorPage` + `ConsentTemplates`. **index.jsx had latent bugs** (referenced `React` + `ConsentGeneratorPage` as values while only re-exporting them — would throw in strict mode) — avoided by importing the component in client.js. server/index.js loads methods.js + routes.js (the addFiles set; shared lib/ConsentTemplates.js imported by both). No Npm.depends, no Atmosphere-isms, MUI v5. `shield`→`Shield`. Not in `.meteor/packages`; 0 importers. Monorepo-tracked → fresh git init.

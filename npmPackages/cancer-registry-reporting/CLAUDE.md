# CLAUDE.md — @node-on-fhir/cancer-registry-reporting

Migrated from Atmosphere `clinical:cancer-registry-reporting` (2026-06-13). ONC
170.315(f)(4) — cancer registry reporting. Route `/cancer-registry-reporting`.
Monorepo-tracked → fresh `git init`; SidebarWorkflows + ClinicianWorkflows →
sidebarItems (`biotech`→`Biotech`, `science`→`Science`); methods-only server.

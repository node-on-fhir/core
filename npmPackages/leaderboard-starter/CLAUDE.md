# CLAUDE.md — @node-on-fhir/leaderboard-starter

Migrated from Atmosphere `mitre:leaderboard-starter` (2026-06-13, Apache-2.0 preserved). SMART-on-FHIR leaderboard demo. Route `/leaderboard`. The Atmosphere `MainPage` (path /) override is preserved as a named export but NOT in the default-export routes (so it does not hijack the homepage via EXTRA_WORKFLOWS). Sidebar relabeled to "Leaderboard" (original mislabeled "International Patient Summary"); icon `Leaderboard`. assets/ carried; json5/prop-types peers (present). Monorepo-tracked → fresh git init.

# CLAUDE.md — @node-on-fhir/structured-data-capture

Migrated from Atmosphere `clinical:structured-data-capture` (2026-06-13, MIT). FHIR Structured Data Capture — Questionnaire/QuestionnaireResponse forms, builder, library, response analytics. 5 routes (all requireAuth). Atmosphere client mainModule was index.jsx (clean ES-module style, routes used `component:` refs → mapped here via workflow.json `component` names + a COMPONENTS map). Self-contained client.js preserves named exports: `SidebarWorkflows` ([] — items commented out upstream), `ClinicianWorkflows` (4 — the active nav flavor, used as the default `sidebarItems`), `FooterButtons` (label/onClick config style), `ModuleConfig`, plus the lib surface (QuestionnaireForm, QuestionnaireUtils/ResponseUtils/ValidationUtils — all `export const`). server/index.js imports the 3 shared utils + methods.js (Atmosphere server mainModule). No Npm.depends. lowercase icons mapped: `notepad`→`Assignment`, `ic_hearing`→`Hearing`, `document`→`Description`. `example.jsx` + `tests/` skipped. Already commented out in `.meteor/packages`. Monorepo-tracked → fresh git init.

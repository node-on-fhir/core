# CLAUDE.md — @node-on-fhir/reference-app

Migrated from Atmosphere `clinical:reference-app` (2026-06-13, MIT). ONC (g)(10) reference application + certification tracking. 2 routes (`/reference-app`, `/g10-certification`, requireAuth) via workflow.json + COMPONENTS map. Self-contained client.js preserves the full export surface: `AdminDynamicRoutes`, `AdminSidebarElements`, `FooterButtons` (→ ReferenceAppFooterButtons, also wired via the default-export `footerButtons`), `FooterElements`, `MainPage` (`/` override — **named export only, not in routes**, like patient-chart-starter), `ModuleConfig`, `PatientsDirectoryButtons` (Certify → Session + Meteor.navigate to /g10-certification), `WorkflowTabs`, plus components + `ReferenceAppCollections`/`ReferenceAppUtilities`. Two settings gates preserved (`referenceApp.enabled`, `referenceApp.showInSidebar`). **Strict-mode fixes:** added the `Meteor` import (index.jsx used it as an Atmosphere global) and declared `SidebarElements` (a gate assigns to it). Kept server/index.js mainModule (imports methods.js + publications.js + startup).

**SERVER-ASSET GOTCHA (the heavy part):** `server/methods.js` read the Daisey 367-resource reference bundle via `await Assets.getTextAsync('data/Daisy/Daisey627_…json')` at 3 sites — NPM packages have no Assets API. Converted to a direct `import daiseyBundle from '../data/Daisy/Daisey627_…json'` (Rspack inlines the JSON); the 3 reads now use `JSON.stringify(daiseyBundle)` to preserve the "bundleJson is text" contract for the downstream `JSON.parse` / direct-download paths. The `data/Daisy/*.json` bundle files are kept (imported); `*.py`/`*.md` + `ReferenceAppPage_backup.jsx` skipped. The 27 `{weak:true}` `clinical:*` api.use entries were optional ONC integrations → dropped. lib/collections.js collections are `Meteor.isClient`-guarded + exported at bottom (safe); SimpleSchema block stays commented. Commented out in `.meteor/packages`. `imports/ui/Footer.jsx` did a runtime `Package[name].FooterButtons` lookup (Atmosphere) → superseded by WorkflowRegistry `footerButtons`; no hard ES importer. Monorepo-tracked → fresh git init.

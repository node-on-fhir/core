// npmPackages/personal-characteristics/server.js
//
// This package is client-only (the Atmosphere original declared no server
// files). This minimal entry exists so the manifest's `serverEntry: ./server`
// resolves cleanly instead of defaulting to the non-existent ./server/methods.
console.log('[personal-characteristics] Server entry loaded (no server-side logic)');

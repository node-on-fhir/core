# CLAUDE.md — @node-on-fhir/admin-tools

Migrated from Atmosphere `clinical:admin-tools` (2026-06-13, MIT). Admin tools — sessions, database admin, and settings-gated patient lifecycle ops (delete/archive/rename/anonymize). Reference implementation for the **settings-gated-feature** pattern: server methods `adminTools.checkArchivalSetting`/`checkRenameSetting`/`checkAnonymizationSetting` read `settings.private.allowPatient{Archival,Rename,Anonymization}`. 8 routes (`element:` → workflow.json + COMPONENTS map; settings gate on `settings.public.modules.adminTools.enabled` empties routes/sidebars/buttons). Self-contained client.js preserves SidebarWorkflows (7) + AdminSidebarElements (mirror) + PatientsDirectoryButtons (inline icon) + CollectionAdminMethods + scanner utils + the 7 page components + AdminToolsCollections. Kept server/index.js mainModule (imports the 5 method files + startup). Clean ES6, no Atmosphere-isms, MUI v5, no assets. Icons already PascalCase.

**Decommission-coupling note (order-catalog shape):** `packages/data-exporter`'s `lib/MedicalRecordsExporter.js` imports `meteor/clinical:admin-tools/lib/{Anonymizer,PatientCompartmentMapper}`. This package exposes `./lib/Anonymizer`/`./lib/PatientCompartmentMapper`/`./lib/collections`/`./lib/AdminMethodsScanner` subpath exports so those consumers can repoint to `@node-on-fhir/admin-tools/lib/…`. **The repoint is DEFERRED to data-exporter's own migration** — data-exporter is a *separate nested repo* (its own `.git`) and is unmigrated/inactive (not in `.meteor/packages`), so editing it from here would couple two repos; the dangling `meteor/clinical:admin-tools` import is harmless until data-exporter is built. Not in `.meteor/packages`. Monorepo-tracked → fresh git init.

# CLAUDE.md — @node-on-fhir/healthcare-surveys

Migrated from Atmosphere `healthcare-surveys` (2026-06-13, MIT). Public-health surveys transmission (ONC §170.315(f)(7)). Route `/healthcare-surveys` (requireAuth). Atmosphere client mainModule was index.jsx (clean ES-module style); consolidated into self-contained client.js (route/sidebar from workflow.json; preserves SidebarElements + SidebarWorkflows). Server: `api.addFiles('server/methods.js')` was the ONLY wired server file; methods.js is self-contained (Meteor.methods only). The `server/{methods,publications,cron,fhir}/` subtrees + `imports/api/*` were present but never wired into package.js (**dead code** — kept for fidelity but not imported, so unbundled). The 9 `lib/schemas/*` are likewise unimported by the loaded graph; their Atmosphere bare-global `XSchema = new SimpleSchema(...)` / `X = new Mongo.Collection(...)` decls were nonetheless converted to `const X = globalThis.X = …` and their `if (typeof exports==='object') { module.exports = … }` CJS guards to ESM `export {…}`, so they are safe if ever imported. No Npm.depends; simpl-schema peer. `guide/` (HL7 submodule), `tests/` skipped. `analytics`→`Analytics`, `publish`→`Publish`. Not in `.meteor/packages`. Submodule → fresh git init.

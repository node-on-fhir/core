._*

node_modules/

*.galaxy.*
version.desktop

.desktop
.desktop/*

logs
logs/*

test
tests_output
tests/output
tests/screenshots
tests/nightwatch/screenshots
tests_output

# Packages are separate git repos — ignore contents, but track the directory-level CLAUDE.md guides
packages/*
!packages/CLAUDE.md
npmPackages/*
!npmPackages/CLAUDE.md

# extensions/* are private user-defined packages with their own nested repos
# (core/* is deliberately NOT ignored — core packages are tracked in this monorepo)
extensions/*
!extensions/CLAUDE.md

# Retired Atmosphere packages parked post-migration (each keeps its own nested git repo)
deprecated/

.DS_Store

# Environment files (contain secrets)
.env
.env.*
!.env.*.example

.meteor/packages

settings.*.json

screenshots/*
screenshots/*/*

scrap
workzone


proprietary
contracted

# Meteor Modern-Tools build context directories
_build
*/build-assets
*/build-chunks
.rsdoctor

# Auto-generated workflow barrels (generated by rspack.workflowParser.js)
imports/workflows/index.js
imports/workflows/loader.js
imports/workflows/server-loader.js

# Auto-generated workflow assets (copied by rspack.workflowParser.js)
public/workflows/

# CesiumJS static assets (copied by scripts/copy-cesium-assets.js)
public/cesium/


# Ralph loop ephemeral state
.claude/ralph-loop.local.md
